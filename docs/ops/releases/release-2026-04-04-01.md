# Release 2026-04-04-01

状态：`draft`

说明：

- 这是本次发布的工作单
- 当前还未冻结最终 `git tag` 和 `commit`
- 在冻结之前，本文档只能作为发布准备稿，不能作为正式执行版本

## 1. 发布基线

- `release_id`: `release-2026-04-04-01`
- `environment`: `prod`
- `git_tag`: `待冻结`
- `git_commit`: `待冻结`
- `rollback_tag`: `待确认`
- `owner`: `待指定`
- `reviewer`: `待指定`
- `window`: `待指定`
- `status`: `draft`

## 2. 本次目标

本次发布目标是把当前一批桌面端、控制面和 OEM 运行时相关改动，按统一基线一次性发布到 `prod`，避免继续出现：

- 代码已更新，但 runtime 配置未更新
- 资源已更新，但控制面 preset 未同步
- 前端 / 后端 / MinIO / 数据库各自为政

## 3. 预估发布范围

以下是当前预估范围，正式发布前必须在冻结基线时逐项确认。

### 3.1 Code

- [x] `services/control-plane`
- [x] `admin-web`
- [x] `home-web`
- [x] `apps/desktop`

说明：

- 这次讨论里，存在多个横跨 `desktop / control-plane / admin-web / home-web` 的改动
- 冻结时必须以 `git diff <rollback_tag>..<git_tag>` 为准重新核对

### 3.2 DB Schema

- [ ] 暂未确认有 schema 变更

说明：

- 若最终基线内包含 migration、索引或表结构调整，必须补充到本节

### 3.3 DB Data

- [x] OEM runtime / welcome 配置修正
- [x] preset 同步
- [x] 可能存在模型中心、记忆配置、品牌配置等数据变更

待确认项：

- 这次基线是否包含需要同步到 `prod` 的模型中心配置
- 这次基线是否包含需要同步到 `prod` 的 Memory provider 配置
- 这次基线是否包含 OEM shell / welcome / 输入框配置数据

### 3.4 Object Storage

- [x] OEM 资源
- [x] desktop 下载产物
- [x] 可能存在 runtime 资源同步

待确认项：

- 本次是否需要同步 `MinIO` 全量桶
- 本次是否需要只同步新增对象
- 本次是否需要发布新的 installer / updater / manifest

### 3.5 Infra Config

- [ ] 暂未确认有新的 infra 配置

待确认项：

- 是否涉及 Nginx path / 域名 / HTTPS / PM2 配置调整

## 4. 正式执行前必须冻结的内容

1. 冻结发布基线
   - 选定唯一 `git commit`
   - 创建唯一 `git tag`
2. 列出本次影响对象
   - `Code`
   - `DB Schema`
   - `DB Data`
   - `Object Storage`
   - `Infra Config`
3. 明确回滚目标
4. 明确执行人和复核人

未完成以上 4 项前，不允许执行 `prod` 发布。

## 5. 推荐执行顺序

1. 冻结基线
2. 执行 `DB Schema`
3. 执行 `DB Data`
4. 发布 `control-plane`
5. 发布 `admin-web`
6. 发布 `home-web`
7. 同步 `MinIO / S3`
8. 发布 desktop 安装包 / updater / 下载页
9. 执行 smoke test
10. 记录结果

## 6. 推荐命令清单

以下命令是候选动作，正式执行前要按冻结基线裁剪。

### 6.1 控制面

```bash
bash scripts/deploy-control-plane.sh prod
```

注意：

- 当前脚本只同步 `src/`、`scripts/`、`sql/`
- 若本次发布包含 `services/control-plane/presets/`，必须先补脚本或单独同步

### 6.2 管理后台

```bash
bash scripts/deploy-admin.sh prod
```

### 6.3 官网 / 下载页

```bash
bash scripts/deploy-home.sh prod
```

### 6.4 数据库

```bash
bash scripts/sync-control-plane-db-to-prod.sh
```

注意：

- 这是高风险动作
- 当前脚本偏向整段 schema 同步，不是细粒度 migration
- 正式执行前必须确认是否真的需要整段同步，而不是局部 migration / data patch

### 6.5 对象存储

```bash
bash scripts/sync-minio-to-prod.sh
```

注意：

- 必须先确认本次需要同步的 bucket
- 不允许盲目全量覆盖

### 6.6 Desktop 发布

```bash
pnpm publish:desktop-release
```

如果本次需要重新打包，也应先完成矩阵构建和上传清单确认。

## 7. 本次已知风险

- `control-plane` 发布脚本当前未覆盖 `presets/`，容易导致代码和 preset 不一致
- 数据库同步脚本偏粗，不适合所有发布都直接整段恢复
- 当前桌面端 / OEM / 资源链路跨 `DB + code + asset`，如果不按顺序发，极易出现“代码已改但配置未生效”
- 本次工作区内仍有大量进行中改动，正式发布前必须重新冻结范围

## 8. Smoke Test 清单

- [ ] `control-plane /health`
- [ ] `admin-web` 正常打开
- [ ] `home-web` 正常打开
- [ ] `portal/public-config?app_name=<brand>` 返回预期配置
- [ ] 品牌 welcome 页资源清晰可见
- [ ] 登录 / 注册正常
- [ ] chat 发送一条真实消息成功
- [ ] usage / 计费 / 余额变更正确
- [ ] desktop 下载页和安装包链接正常
- [ ] updater manifest 可读取

## 9. 回滚预案

### 9.1 Code

- 使用 `rollback_tag` 对应版本重新发布

### 9.2 DB

- 若为 migration，按 migration 回滚策略处理
- 若为 data patch，执行对应反向 SQL 或恢复备份

### 9.3 Object Storage

- 恢复上一版对象
- 恢复上一版下载清单

## 10. 冻结时要补齐的字段

正式执行前，必须补齐以下内容：

- `git_tag`
- `git_commit`
- `rollback_tag`
- 执行人 / 复核人
- 最终发布范围
- 最终执行命令
- 最终回滚命令
