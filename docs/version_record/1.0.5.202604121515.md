# 1.0.5.202604121515

## 1. 发布基线

- `version_no`: `1.0.5.202604121515`
- `release_id`: `release-2026-04-12-01`
- `environment`: `prod`
- `git_tag`: `release-1.0.5.202604121515`
- `git_commit`: `4c2c2a4a`
- `rollback_tag`: `1.0.4.202604101315（当前已知上一正式版本）`
- `owner`: `xingkaihan`
- `reviewer`: `未指定`
- `window`: `2026-04-12 15:15:15 CST`
- `test_report`: `docs/version_record/test_report/1.0.5.202604121515.md`
- `status`: `done`

## 2. 变更摘要

- 本次发布目标：推进 `1.0.5.202604121515`，发布新版 `control-plane`、React 化 `admin-web`、桌面端运行时恢复与故障上报链路，并按新版本更新桌面安装包与下载页。
- 用户可见变化：
  - `admin-web` 持续从旧单文件实现迁移到 `src/` 结构化页面
  - 桌面端登录后不再因附带同步失败误报登录失败
  - 桌面端聊天/定时任务恢复、全局异常与故障上报链路补强
  - 投资专家 / Lobster 等桌面体验继续收敛
- 风险最高的点：
  - `admin-web/main.js` 被删除，必须确保新版 `admin-web/src/` 构建产物完整替代
  - 本次包含 `DB Schema`，必须先发 migration 再发服务
  - 如桌面包对外版本切到 `1.0.5`，下载页与对象存储必须同步

## 3. 发布范围

### 3.1 Code

- [x] `control-plane`
- [x] `admin-web`
- [ ] `home-web`
- [x] `desktop`
- [ ] 其他：

### 3.2 DB Schema

- [ ] 无
- [x] 有

内容：

- 新增桌面故障上报表：
  - `services/control-plane/sql/005_desktop_fault_reports.sql`

### 3.3 DB Data

- [ ] 无
- [x] 有

内容：

- `platform-db.snapshot` 有变更，需确认是否需要同步 baseline 真值
- 如涉及投资专家候选 / 平台技能真值，发布前需确认数据库是否已对齐

### 3.4 Object Storage

- [ ] 无
- [x] 有

内容：

- 重新构建并上传 `iclaw` / `licaiclaw` 本次 `desktop` 正式安装包
- 如切下载页，对应 `home-web` 也需更新公开版本号

### 3.5 Infra Config

- [x] 无
- [ ] 有

## 4. 发布前检查

- [x] 已冻结新版本号 `1.0.5.202604121515`
- [x] 已生成测试报告并落到 `docs/version_record/test_report/`
- [x] 发版文档已填写 `test_report` 字段
- [ ] 已确认数据库为真值且 snapshot 已同步
- [x] 已确认发布范围
- [x] 已确认回滚目标
- [x] 已确认脚本默认目标环境正确
- [ ] 已冻结 `git tag + commit`

## 5. 执行顺序

1. 版本推进到 `1.0.5.202604121515`
2. 提交版本冻结并创建 `release-1.0.5.202604121515`
3. 执行 `DB Schema`
4. 评估并执行 `DB Data`
5. 发布 `control-plane`
6. 发布 `admin-web`
7. 构建 `iclaw` / `licaiclaw` 桌面正式包
8. 上传对象存储内容
9. 如需要，重新发布 `home-web`
10. 执行 smoke 验证并回填本记录

## 6. 执行命令

### 6.1 版本推进

```bash
bash scripts/version.sh 1.0.5 202604121515
git commit -m "chore: bump release to 1.0.5.202604121515"
git tag release-1.0.5.202604121515
```

### 6.2 DB Schema

```bash
psql "$DATABASE_URL" -f services/control-plane/sql/005_desktop_fault_reports.sql
```

### 6.3 Backend / Admin

```bash
bash scripts/deploy-control-plane.sh prod
bash scripts/deploy-admin.sh prod
```

### 6.4 Desktop / Downloads / Home

```bash
pwsh -File scripts/release-desktop-windows.ps1 -Brand iclaw -ReleaseVersion 1.0.5.202604121515 -Channels prod -Targets x86_64-pc-windows-msvc -SkipHomeDeploy
pwsh -File scripts/release-desktop-windows.ps1 -Brand licaiclaw -ReleaseVersion 1.0.5.202604121515 -Channels prod -Targets x86_64-pc-windows-msvc -SkipHomeDeploy
```

## 7. 风险点

- 本次 `admin-web` 是大体量迁移，不是单点修补
- `desktop_fault_reports` migration 若漏发，后台与桌面故障链路会出现接口/存储不一致
- 若下载页不切版本，用户仍会下载到旧桌面包

## 8. 验证项

- `test_report`: `docs/version_record/test_report/1.0.5.202604121515.md`
- `minio_prefix`: `prod object storage /downloads/windows/x64/`
- [ ] `control-plane /health`
- [ ] `admin-web` 可访问
- [ ] `home-web` 可访问（若本次发布）
- [ ] `control-plane /health` 返回 `git_commit / git_tag / release_version / build_time`
- [ ] `admin-web/build-info.json` 字段完整
- [ ] `desktop` 登录 / 注册正常
- [ ] chat 发消息正常
- [ ] usage / 计费 / 余额链路正常
- [ ] OEM runtime 拉取正常
- [x] 下载页可访问
- [x] 安装包链接不 404

## 9. 回滚方案

### 9.1 Code

- `control-plane` / `admin-web` / `desktop` 回滚到 `1.0.4.202604101315` 对应稳定基线

### 9.2 DB

```sql
drop table if exists desktop_fault_reports;
```

### 9.3 Object Storage

- 恢复方式：
  - 下载页回切到 `1.0.4.202604101315`
  - 安装包对象恢复上一稳定版本

## 10. 实际执行记录

- 开始时间：`2026-04-12 15:15:15 CST`
- 结束时间：`2026-04-12 23:20 CST`
- 实际执行人：`xingkaihan`
- 实际结果：`done`
- 偏差说明：
  - macOS 包已先在 Mac 宿主完成；本次仅在 Windows 宿主补齐 `iclaw` / `licaiclaw` 的 `prod x64` 厚包并上传
  - `iclaw` Windows 包大小约 `93.33 MB`
  - `licaiclaw` Windows 包大小约 `77.21 MB`
  - 对外下载地址已切到：
    - `https://iclaw.aiyuanxi.com/downloads/windows/x64/iClaw_1.0.5.202604121515_x64_prod.exe`
    - `https://caiclaw.aiyuanxi.com/downloads/windows/x64/LiCaiClaw_1.0.5.202604121515_x64_prod.exe`
  - 本轮打包中修复了两个实际阻塞：
    - Windows 宿主不再执行 mac 专用 `generate-icons.sh`
    - `release-desktop-windows.ps1` 兼容无 `Start-Process -Environment` 的宿主
  - 编译期还修复了一处 Rust 所有权错误，否则桌面包无法完成构建
