# 1.0.9.202604171540

## 1. 发布基线

- `version_no`: `1.0.9.202604171540`
- `environment`: `prod`
- `owner`: `admin`
- `window`: `2026-04-17 15:41 CST`
- `test_report`: `docs/version_record/test_report/1.0.9.202604171540.md`
- `status`: `draft`

## 2. 变更摘要

- 修复 `services/control-plane` 同步任务相关 TypeScript 编译错误，恢复发布链路可构建状态。
- 固化 `caiclaw` Windows `prod` 发布脚本，按显式 `channel/env` 读取品牌配置，避免误读 `dev` 包装配置导致上传到错误 MinIO bucket。
- 为 Windows 发布脚本补充公共下载地址回读校验，发布完成后自动验证 `latest-*.json` 与安装包公开地址是否可访问。
- 将 `caiclaw` 的 `prod/test` 下载 bucket 与 runtime bucket 对齐到现网真实基础设施 `licaiclaw-prod`。

## 3. 发布产物

- Windows x64 prod 安装包：`dist/releases/LiCaiClaw_1.0.9.202604171540_x64_prod.exe`
- 公共清单：`dist/releases/latest-prod.json`
- Windows x64 清单：`dist/releases/latest-prod-windows-x64.json`

## 4. 验证结果

- `pnpm --filter @iclaw/admin-web build` 通过。
- `$env:APP_NAME='caiclaw'; $env:ICLAW_PORTAL_APP_NAME='caiclaw'; $env:ICLAW_BRAND='caiclaw'; $env:ICLAW_ENV_NAME='prod'; pnpm --filter @iclaw/desktop build` 通过。
- `powershell -ExecutionPolicy Bypass -File scripts/release-desktop-windows.ps1 -Brand caiclaw -ReleaseVersion 1.0.9.202604171540 -Channels prod -Targets x86_64-pc-windows-msvc -SkipBuild -SkipHomeDeploy` 通过。
- 公网下载地址验证通过：
  - `https://caiclaw.aiyuanxi.com/downloads/latest-prod.json`
  - `https://caiclaw.aiyuanxi.com/downloads/windows/x64/latest-prod-windows-x64.json`
  - `https://caiclaw.aiyuanxi.com/downloads/windows/x64/LiCaiClaw_1.0.9.202604171540_x64_prod.exe`

## 5. 风险与备注

- `caiclaw.aiyuanxi.com` 当前下载目录仍映射到对象存储 bucket `licaiclaw-prod/downloads/`，不是字面上的 `caiclaw-prod`。后续若迁移基础设施，需要同步调整品牌配置与发布脚本验证。
- 本次已将“错误读取默认 env 导致传错 bucket”的坑沉淀为脚本显式参数与发布后自动校验，后续不应再依赖人工热修复。
