# Test Report — 1.0.5.202604130925

## 1. 基本信息

- `version_no`: `1.0.5.202604130925`
- `environment`: `prod`
- `related_version_record`: `docs/version_record/1.0.5.202604130925.md`

## 2. 本次验证

- `iClaw` macOS arm64 prod 包完成签名、公证、staple
- `LiCaiClaw` macOS arm64 prod 包完成签名、公证、staple
- 两个 DMG 已上传到各自 prod 下载 bucket
- 两个公网 DMG 直链返回 `HTTP 200`
- 两个公网 manifest 已更新为：
  - `release_version = 1.0.5.202604130925`
  - `updater = null`

## 3. 关键技术结论

- 厚包架构已生效：
  - 安装包携带 runtime archive
  - 包内无展开态 `openclaw-runtime/`
- macOS notarization 能通过的关键条件：
  - runtime archive 内部所有 Mach-O 必须补 Developer ID 签名
  - Python framework 需要按 bundle root 补签
  - bundled skills 必须剔除 `.git`，否则 Tauri bundling 阶段 `xattr -cr` 会失败
