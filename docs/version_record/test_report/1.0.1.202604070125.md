# Test Report — 1.0.1.202604070125

## 1. 基本信息

- `version_no`: `1.0.1.202604070125`
- `release_id`: `prod-db-data-fix-2026-04-07-01`
- `environment`: `prod`
- `report_owner`: `xingkaihan`
- `generated_at`: `2026-04-07`
- `related_version_record`: `docs/version_record/1.0.1.202604070125.md`
- `minio_prefix`: `dev/test_report/1.0.1.202604070125/`

## 2. 测试范围

- 本次覆盖模块：
  - prod `cloud_mcp_catalog`
  - prod 公网 MCP catalog 接口 `/mcp/catalog`
- 本次不覆盖模块：
  - 登录 / 注册
  - chat
  - usage / billing
  - desktop 安装包
  - 对象存储
- 与发版范围的对应关系：
  - 本次为 `DB Data only` 线上修复，验证目标是“数据库已写入 + 用户可见接口已返回”

## 3. 执行环境

- 前端地址：`https://iclaw.aiyuanxi.com`
- 后端地址：`https://iclaw.aiyuanxi.com`
- control-plane：`prod host 115.191.6.179`
- CDP / 浏览器环境：`未使用`
- 测试账号：`不涉及`
- 其他依赖：
  - `ssh root@115.191.6.179`
  - prod PostgreSQL
  - `curl https://iclaw.aiyuanxi.com/mcp/catalog`

## 4. 执行用例

| Case ID | 脚本/文档 | 类型 | 结果 | 备注 |
| --- | --- | --- | --- | --- |
| PROD-DB-MCP-001 | `psql app.cloud_mcp_catalog` 回查 | DB data verify | Pass | 确认 `lark` / `dingtalk` / `wecom` 均存在 |
| PROD-API-MCP-001 | `curl https://iclaw.aiyuanxi.com/mcp/catalog` | user-visible smoke | Pass | 确认公网返回 3 条 MCP，且名称为 CLI 口径 |

## 5. 关键结果

- 通过项：
  - prod DB 总数从 `32` 变为 `34`
  - `lark` 已更新为 `飞书 CLI`
  - `dingtalk` / `wecom` 已新增
  - 公网 `/mcp/catalog` 已返回三条记录
- 失败项：
  - 无
- 阻塞项：
  - 无

## 6. 证据

- 截图：无；本次为数据库与接口回查，不强制截图
- 日志：
  - prod DB 回查输出
  - 公网 `/mcp/catalog` 回查输出
- 临时归档：无
- 其他：
  - 三条公网返回的 `updated_at` 均为 `2026-04-06T17:21:38.801Z`（即北京时间 `2026-04-07 01:21:38 CST`）

### 6.1 evidence_objects

- `PROD-DB-MCP-001`:
  - `object_path`: `n/a`
  - `note`: `证据为本次执行日志中的 psql 输出，未上传对象存储`
- `PROD-API-MCP-001`:
  - `object_path`: `n/a`
  - `note`: `证据为本次执行日志中的 curl 输出，未上传对象存储`

## 7. 风险结论

- 剩余风险：
  - 若后续用旧 baseline 再次覆盖 prod DB，三条记录可能被回退，需要在后续基线修复时一并纳入
- 是否允许发布：
  - `不适用（本次不是代码发布，而是 prod DB Data 修复）`
- 说明：
  - 正式“发版前必须跑通 P0”规则适用于代码/包体/对象存储类发布
  - 本次未执行 `P0`，原因是变更范围仅限 prod DB Data，且已完成用户可见接口级 smoke 验证
