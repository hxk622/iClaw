# 1.0.1.202604070235

## 1. 发布基线

- `version_no`: `1.0.1.202604070235`
- `release_id`: `release-2026-04-07-01`
- `environment`: `prod`
- `git_tag`: `release-1.0.1.202604070235`
- `git_commit`: `待冻结`
- `rollback_tag`: `release-1.0.1.202604062034-post2`
- `owner`: `xingkaihan`
- `reviewer`: `未指定`
- `window`: `2026-04-07 02:36:52 CST`
- `test_report`: `docs/version_record/test_report/1.0.1.202604070235.md`
- `status`: `running`

## 2. 变更摘要

- 本次发布目标：按最新时间戳版本号重新发布当前 prod 基线，并补发最新 `control-plane`、`admin-web`、`iclaw`、`licaiclaw`
- 用户可见变化：
  - 支付中心 OEM 网关支持显式 `继承平台 / 使用 OEM 独立网关`
  - 支付网关输入框失焦不再回滚旧值
  - 登录框左上角 logo 改为铺满容器，减少白边
  - 对外下载版本统一切到新的四段点号时间戳
- 风险最高的点：
  - `control-plane` 与 `admin-web` 的 build metadata 必须和新版本号一致
  - `iclaw` / `licaiclaw` 的 DMG 与下载页必须同步切到同一时间戳

## 3. 发布范围

### 3.1 Code

- [x] `control-plane`
- [x] `admin-web`
- [x] `home-web`
- [x] `desktop`

### 3.2 DB Schema

- [x] 无
- [ ] 有

### 3.3 DB Data

- [ ] 无
- [x] 有

内容：

- 同步 prod 系统覆盖项：
  - `payment_gateway:epay`
  - `payment_gateway:epay:app:iclaw`

### 3.4 Object Storage

- [ ] 无
- [x] 有

内容：

- 上传 `iclaw` / `licaiclaw` 本次新版本 `prod darwin/aarch64` DMG

### 3.5 Infra Config

- [x] 无
- [ ] 有

## 4. 发布前检查

- [x] 已冻结新版本号 `1.0.1.202604070235`
- [x] 已确认回滚目标为 `release-1.0.1.202604062034-post2`
- [x] 已确认本次只发 `macOS arm64`
- [x] 已确认需要同步 `control-plane` / `admin-web` / `desktop` / 下载页
- [x] 已生成测试报告并落到 `docs/version_record/test_report/`

## 5. 执行顺序

1. 版本推进到 `1.0.1.202604070235`
2. 提交版本冻结并创建 `release-1.0.1.202604070235`
3. 发布 `control-plane`
4. 发布 `admin-web`
5. 构建 `iclaw` `prod aarch64-apple-darwin`
6. 构建 `licaiclaw` `prod aarch64-apple-darwin`
7. 上传两个品牌 DMG 到各自下载桶
8. 重新发布两个品牌 `home-web prod`
9. 执行 smoke 验证并回填本记录

## 6. 执行命令

### 6.1 版本推进

```bash
bash scripts/version.sh 1.0.1 202604070235
git commit -m "chore: bump release to 1.0.1.202604070235"
git tag release-1.0.1.202604070235
```

### 6.2 Backend / Admin

```bash
bash scripts/deploy-control-plane.sh prod
bash scripts/deploy-admin.sh prod
```

### 6.3 Desktop / Downloads / Home

```bash
node scripts/build-desktop-package.mjs --target aarch64-apple-darwin
bash scripts/publish-downloads.sh prod
pnpm --dir home-web build
```

## 7. 风险点

- `macOS x64` 仍不在正式发版范围内
- 下载页如只替换文件不切版本号，会继续暴露旧时间戳
- `control-plane` 远端目录不是 git 仓库，发布后需以 `build-info.json` 和 `/health` 为准校验

## 8. 验证项

- `test_report`: `docs/version_record/test_report/1.0.1.202604070235.md`
- [ ] `control-plane /health` 返回 `git_commit / git_tag / release_version`
- [ ] `admin-web/build-info.json` 返回同一基线
- [ ] `iclaw` DMG 下载链接 `200 OK`
- [ ] `licaiclaw` DMG 下载链接 `200 OK`
- [ ] `iclaw` 下载页 `release.version="1.0.1.202604070235"`
- [ ] `licaiclaw` 下载页 `release.version="1.0.1.202604070235"`

## 9. 回滚方案

### 9.1 Code

- `control-plane` / `admin-web` / `home-web` 回滚到 `release-1.0.1.202604062034-post2`

### 9.2 Desktop

- 下载页回切到上一版 `1.0.1.202604062034`
- 如需回滚安装包对象，恢复上一版 DMG

### 9.3 DB

- 本次无 schema 回滚
- 如需恢复支付网关数据，以发布前系统覆盖项为准重新回灌

## 10. 实际执行记录

- 开始时间：`2026-04-07 02:36:52 CST`
- 结束时间：
- 实际执行人：`xingkaihan`
- 实际结果：`进行中`
- 偏差说明：
