# 1.0.1.202604070125

## 1. 发布基线

- `version_no`: `1.0.1.202604070125`
- `release_id`: `prod-db-data-fix-2026-04-07-01`
- `environment`: `prod`
- `git_tag`: `n/a (db-data-only)`
- `git_commit`: `n/a (db-data-only)`
- `rollback_tag`: `n/a (db-data-only)`
- `owner`: `xingkaihan`
- `reviewer`: `未指定`
- `window`: `2026-04-07 01:21:00 CST`
- `test_report`: `docs/version_record/test_report/1.0.1.202604070125.md`
- `status`: `completed`

## 2. 变更摘要

- 本次变更目标：修复 prod 的 MCP catalog 数据缺口，把 `飞书 CLI`、`钉钉 CLI`、`企微 CLI` 三个协作类 MCP 补齐到线上数据库
- 用户可见变化：
  - MCP 商店接口 `/mcp/catalog` 可返回：
    - `lark` → `飞书 CLI`
    - `dingtalk` → `钉钉 CLI`
    - `wecom` → `企微 CLI`
- 风险最高的点：
  - 这是一次线上数据库手工数据修复，不是代码发布；若未留档，后续容易出现“线上有、仓库记录没有”的追溯断点

## 3. 发布范围

### 3.1 Code

- [x] 无
- [ ] `control-plane`
- [ ] `admin-web`
- [ ] `home-web`
- [ ] `desktop`

### 3.2 DB Schema

- [x] 无
- [ ] 有

内容：

- 无 schema 变更

### 3.3 DB Data

- [ ] 无
- [x] 有

内容：

- 在线上 `app.cloud_mcp_catalog` 执行定向 upsert：
  - `lark`
  - `dingtalk`
  - `wecom`
- 修复前确认：
  - `lark` 已存在，但名称为 `飞书`
  - `dingtalk` / `wecom` 不存在
- 修复后确认：
  - 三条均存在，且名称统一为 CLI 口径

### 3.4 Object Storage

- [x] 无
- [ ] 有

### 3.5 Infra Config

- [x] 无
- [ ] 有

## 4. 发布前检查

- [x] 已确认这是 `DB Data only` 线上修复
- [x] 已生成测试报告并落到 `docs/version_record/test_report/`
- [x] 发版文档已填写 `test_report` 字段
- [x] 已确认不涉及代码 / schema / 对象存储 / infra 发布
- [x] 已确认执行机为 prod 主机 `115.191.6.179`

## 5. 执行顺序

1. 登录 prod 主机
2. 确认 prod 实际 `DATABASE_URL`
3. 查询 `cloud_mcp_catalog` 现状
4. 定向 upsert 三条 MCP 数据
5. 回查 prod DB
6. 回查公网 `/mcp/catalog`
7. 补 version record / test report

## 6. 执行命令

### 6.1 查询 prod 实际数据库配置

```bash
ssh root@115.191.6.179 'cd /opt/iclaw && grep "^DATABASE_URL=" .env.prod'
```

### 6.2 查询 prod 当前记录

```bash
ssh root@115.191.6.179 'cd /opt/iclaw && DATABASE_URL=$(grep "^DATABASE_URL=" .env.prod | cut -d= -f2-) && psql "$DATABASE_URL" -c "select count(*) as total from app.cloud_mcp_catalog;"'
```

### 6.3 执行 DB Data 修复

```bash
ssh root@115.191.6.179 'cd /opt/iclaw/services/control-plane && node --input-type=module <<'"'"'NODE'"'"'
// 使用 pg 直连 prod DATABASE_URL
// 对 app.cloud_mcp_catalog 的 lark / dingtalk / wecom 执行 upsert
NODE'
```

### 6.4 回查接口

```bash
curl -sS https://iclaw.aiyuanxi.com/mcp/catalog
```

## 7. 风险点

- 这是线上手工数据修复，若后续 baseline 数据再次覆盖 prod，需要确认这三条不会被旧基线回滚掉
- 当前 `core-oem.json` 仍保留同类定义；后续如继续以数据库为准，需要避免“文件内容”和“线上实际数据”再次漂移
- 本次未执行完整发布流程，因此不应把它误标成一次代码版本发布

## 8. 验证项

测试报告：

- `test_report`: `docs/version_record/test_report/1.0.1.202604070125.md`
- `minio_prefix`: `dev/test_report/1.0.1.202604070125/`
- [x] 测试报告已与本次变更文档一一对应
- [x] 测试报告已覆盖本次 DB Data 修复的用户可见验证范围
- [ ] `P0` 优先级正式用例已全部跑通
- [x] 本次为 `DB Data only` 修复，`P0` 豁免已在测试报告中说明
- [x] 关键证据已在测试报告中留档

- [x] prod `cloud_mcp_catalog` 总数已从 `32` 变为 `34`
- [x] prod DB 中存在：
  - `lark` → `飞书 CLI`
  - `dingtalk` → `钉钉 CLI`
  - `wecom` → `企微 CLI`
- [x] `https://iclaw.aiyuanxi.com/mcp/catalog` 返回这三条 MCP
- [x] 三条 MCP 的公网返回均为 `transport=http`

## 9. 回滚方案

### 9.1 Code

- 无需代码回滚

### 9.2 DB

```sql
-- 如需回滚到修复前状态：
delete from app.cloud_mcp_catalog where mcp_key in ('dingtalk', 'wecom');
update app.cloud_mcp_catalog
set name = '飞书',
    updated_at = now()
where mcp_key = 'lark';
```

### 9.3 Object Storage

- 无

## 10. 实际执行记录

- 开始时间：`2026-04-07 01:21:00 CST`
- 结束时间：`2026-04-07 01:26:00 CST`
- 实际执行人：`xingkaihan`
- 实际结果：
  - 已在 prod 主机 `115.191.6.179` 上直接修复 `app.cloud_mcp_catalog`
  - 已补入 `dingtalk` / `wecom`
  - 已将 `lark` 名称从 `飞书` 修正为 `飞书 CLI`
  - 已通过 prod DB 与公网 `/mcp/catalog` 双重回查确认生效
- 偏差说明：
  - 本次不是代码发布，不涉及 `git tag` / `git_commit` 冻结
  - 本次未执行正式 `P0` 回归；原因是仅做线上 DB Data 修复，无代码、包体、对象存储和 infra 变更
