# 1.0.3.202604092345

## 1. 发布基线

- `version_no`: `1.0.3.202604092345`
- `release_id`: `release-2026-04-10-01`
- `environment`: `prod`
- `git_tag`: `未创建（流程偏差，事后补录）`
- `git_commit`: `cfd28b9（版本推进提交；正式发版未补单独 freeze 记录）`
- `rollback_tag`: `release-1.0.2.202604090003-post1`
- `owner`: `xingkaihan`
- `reviewer`: `未指定`
- `window`: `2026-04-10 00:00 - 2026-04-10 11:30 CST`
- `test_report`: `docs/version_record/test_report/1.0.3.202604092345.md`
- `status`: `done`

## 2. 变更摘要

- 本次发布目标：推进 `1.0.3.202604092345` 正式版本，完成 `iclaw` / `licaiclaw` Windows 厚包首轮正式切换，并发布共享服务。
- 用户可见变化：
  - `iclaw`、`licaiclaw` 下载页对外版本切到 `1.0.3.202604092345`
  - Windows `prod x64` 产物改为厚包，安装包内直接携带 OpenClaw runtime
  - `control-plane`、`admin-web`、`home-web` 同步到本轮桌面发版基线
- 风险最高的点：
  - 厚包后安装器文件体积、落盘文件数、安装耗时显著增加
  - 同一 Windows 宿主多品牌打包若复用中间态，容易串包
  - 本次缺失 release tag / 冻结单，后续追溯成本高

## 3. 发布范围

### 3.1 Code

- [x] `control-plane`
- [x] `admin-web`
- [x] `home-web`
- [x] `desktop`

### 3.2 DB Schema

- [x] 无
- [ ] 有

### 3.3 DB Data

- [ ] 无
- [x] 有

内容：

- 发布 `iclaw` / `licaiclaw` 桌面正式版本元数据
- 更新两个品牌下载站点对外版本展示
- 调整 Windows 强更口径到本轮正式版本

### 3.4 Object Storage

- [ ] 无
- [x] 有

内容：

- 上传 `iclaw` / `licaiclaw` 本次 `prod windows/x64` 安装包
- 上传对应 manifest / latest 索引

### 3.5 Infra Config

- [x] 无
- [ ] 有

## 4. 发布前检查

- [x] 已冻结发版版本号 `1.0.3.202604092345`
- [x] 已补生成测试报告并落到 `docs/version_record/test_report/`
- [x] 发版文档已填写 `test_report` 字段
- [x] 已确认本次发布范围
- [x] 已确认回滚目标
- [x] 已确认脚本默认目标环境正确
- [ ] 已冻结 `git tag + commit`
- [ ] 已在发布前完成结构化发布单

说明：

- 上述两个未勾选项为本轮真实流程偏差，本记录为事后补录，不做伪装性回填。

## 5. 执行顺序

1. 版本推进到 `1.0.3.202604092345`
2. 调整 Windows 厚包打包脚本与下载发布脚本
3. 构建 `iclaw` `prod x86_64-pc-windows-msvc`
4. 构建 `licaiclaw` `prod x86_64-pc-windows-msvc`
5. 上传两个品牌安装包并切换下载页
6. 发布两个品牌桌面强更策略
7. 发布 `control-plane`
8. 发布 `admin-web`
9. 发布 `iclaw` `home-web`
10. 发布 `licaiclaw` `home-web`
11. 执行 smoke 验证并开始跟踪厚包安装反馈

## 6. 执行命令

### 6.1 版本推进

```bash
bash scripts/version.sh 1.0.3 202604092345
git commit -m "chore: release 1.0.3 desktop packaging hardening"
```

### 6.2 Desktop / Downloads

```bash
APP_NAME=iclaw ICLAW_BRAND=iclaw ICLAW_DESKTOP_TARGETS=x86_64-pc-windows-msvc ICLAW_DESKTOP_CHANNELS=prod bash scripts/build-desktop-matrix.sh 1.0.3.202604092345
APP_NAME=licaiclaw ICLAW_BRAND=licaiclaw ICLAW_DESKTOP_TARGETS=x86_64-pc-windows-msvc ICLAW_DESKTOP_CHANNELS=prod bash scripts/build-desktop-matrix.sh 1.0.3.202604092345
APP_NAME=iclaw ICLAW_BRAND=iclaw bash scripts/publish-downloads.sh prod
APP_NAME=licaiclaw ICLAW_BRAND=licaiclaw bash scripts/publish-downloads.sh prod
APP_NAME=iclaw ICLAW_BRAND=iclaw pnpm publish:desktop-release -- --brand iclaw --channel prod --version 1.0.3+202604092345 --mandatory --disallow-current-run-to-finish --notes "1.0.3 正式发布，需立即升级"
APP_NAME=licaiclaw ICLAW_BRAND=licaiclaw pnpm publish:desktop-release -- --brand licaiclaw --channel prod --version 1.0.3+202604092345 --mandatory --disallow-current-run-to-finish --notes "1.0.3 正式发布，需立即升级"
```

### 6.3 Backend / Frontend

```bash
bash scripts/deploy-control-plane.sh prod
bash scripts/deploy-admin.sh prod
APP_NAME=iclaw ICLAW_BRAND=iclaw bash scripts/deploy-home.sh prod
APP_NAME=licaiclaw ICLAW_BRAND=licaiclaw bash scripts/deploy-home.sh prod
```

## 7. 风险点

- 厚包把 runtime 下载阶段前移到安装器阶段，安装耗时和安全软件干预风险显著上升
- 本次未做 Windows Authenticode 签名，`SmartScreen` 风险未解除，只能按内部测试包口径看待
- 本次未建立安装阶段结构化日志，后续定位 QA 机器问题证据不足
- macOS 正式包不在本轮 Windows 宿主发版范围内

## 8. 验证项

- `test_report`: `docs/version_record/test_report/1.0.3.202604092345.md`
- `minio_prefix`: `未补齐（流程偏差）`
- [x] 测试报告已与本次发布文档一一对应
- [x] 测试报告已覆盖本次发布的用户级验证范围
- [ ] `P0` 优先级正式用例已全部跑通
- [ ] 测试截图等证据已上传到 dev MinIO 并在测试报告中留档
- [x] `control-plane` 已完成同步发布
- [x] `admin-web` 已完成同步发布
- [x] `home-web` 已完成同步发布
- [x] 两个品牌下载页已切到 `1.0.3.202604092345`
- [x] `iclaw` 强更策略已发布
- [x] `licaiclaw` 强更策略已发布
- [ ] QA 自动强更链路完成验证

说明：

- `1.0.3` 发布后发现旧版本客户端未稳定收到强更提示，后续根因分析与新方案沉淀转入 `1.0.4`。

## 9. 回滚方案

### 9.1 Code

- `control-plane` / `admin-web` / `home-web` 回滚到 `release-1.0.2.202604090003-post1`

### 9.2 Desktop

- 将两个品牌下载页和 release manifest 回切到 `1.0.2.202604090003`
- 关闭 `1.0.3` 强更策略，重新发布上一稳定版本为 latest

### 9.3 DB

- 本次无 schema 回滚
- 如需回滚桌面发布元数据，按品牌重新发布上一稳定版本记录

## 10. 实际执行记录

- 开始时间：`2026-04-10 00:00 CST`
- 结束时间：`2026-04-10 11:30 CST`
- 实际执行人：`xingkaihan`
- 实际结果：`done`
- 偏差说明：
  - 本次未按规范先写发布单、再冻结 `tag + commit`
  - 本次未完成签名与 SmartScreen 验证，不应视为正式外发质量完备版本
  - 发布后才暴露出 Windows 厚包安装阶段耗时与可观测性不足问题
