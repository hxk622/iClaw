# 1.0.1.202604061643

## 1. 发布基线

- `version_no`: `1.0.1.202604061643`
- `release_id`: `release-2026-04-06-01`
- `environment`: `prod`
- `git_tag`: `release-1.0.1.202604061643`
- `git_commit`: `77822ba`
- `rollback_tag`: `release-1.0.1.202604041633`
- `owner`: `xingkaihan`
- `reviewer`: `未指定`
- `window`: `2026-04-06 16:43:21 CST`
- `status`: `completed`

## 2. 变更摘要

- 本次发布目标：按当前规范补发 `iclaw` / `licaiclaw` 的 `prod macOS arm64` 业务系统安装包
- 同步目标：把两个品牌下载页统一切到本次最新对外版本 `1.0.1.202604061643`
- 本次明确不覆盖：`macOS x64`、`admin-web`、数据库、对象存储中的非 desktop 下载对象

## 3. 发布范围

### 3.1 Code

- [ ] `control-plane`
- [ ] `admin-web`
- [x] `home-web`
- [x] `desktop`

### 3.2 DB Schema

- [x] 无
- [ ] 有

### 3.3 DB Data

- [x] 无
- [ ] 有

### 3.4 Object Storage

- [ ] 无
- [x] 有

内容：

- 上传 `iclaw` / `licaiclaw` 的 `prod darwin/aarch64` DMG 到各自下载桶

### 3.5 Infra Config

- [x] 无
- [ ] 有

## 4. 发布前检查

- [x] 已冻结 `git tag + commit`
- [x] 已确认发布范围
- [x] 已确认当前 `main` 为最新发布基线
- [x] 已确认本次只发 `macOS arm64`
- [x] 已确认 `macOS x64` 当前不在正式发版范围内

## 5. 执行顺序

1. 冻结发布基线 `77822ba` 并创建 `release-1.0.1.202604061643`
2. 构建 `iclaw` `prod aarch64-apple-darwin`
3. 构建 `licaiclaw` `prod aarch64-apple-darwin`
4. 上传两个品牌的 DMG 到各自 `prod` 下载桶
5. 重新发布 `iclaw` / `licaiclaw` 的 `home-web prod`
6. 验证公网下载地址与下载页版本
7. 记录结果

## 6. 执行命令

### 6.1 Desktop Build

`iclaw`

```bash
env ICLAW_RELEASE_VERSION=1.0.1.202604061643 ICLAW_SKIP_REMOTE_RUNTIME_HASH_VERIFY=1 \
  bash scripts/with-env.sh prod env \
  APP_NAME=iclaw \
  ICLAW_PORTAL_APP_NAME=iclaw \
  ICLAW_BRAND=iclaw \
  NODE_ENV=prod \
  ICLAW_ENV_NAME=prod \
  ICLAW_RELEASE_VERSION=1.0.1.202604061643 \
  ICLAW_SKIP_REMOTE_RUNTIME_HASH_VERIFY=1 \
  ICLAW_OPENCLAW_RUNTIME_URL=https://iclaw.aiyuanxi.com/runtime/openclaw/2026.3.13/openclaw-runtime-aarch64-apple-darwin-2026.3.13.tar.gz \
  ICLAW_OPENCLAW_RUNTIME_SHA256=50a5139e22cd3471eee8569729e835e9fbbabe1b24eaea36b332ec71b617cf35 \
  ICLAW_OPENCLAW_RUNTIME_FORMAT=tar.gz \
  node scripts/build-desktop-package.mjs --target aarch64-apple-darwin
```

`licaiclaw`

```bash
env ICLAW_RELEASE_VERSION=1.0.1.202604061643 ICLAW_SKIP_REMOTE_RUNTIME_HASH_VERIFY=1 \
  bash scripts/with-env.sh prod env \
  APP_NAME=licaiclaw \
  ICLAW_PORTAL_APP_NAME=licaiclaw \
  ICLAW_BRAND=licaiclaw \
  NODE_ENV=prod \
  ICLAW_ENV_NAME=prod \
  ICLAW_RELEASE_VERSION=1.0.1.202604061643 \
  ICLAW_SKIP_REMOTE_RUNTIME_HASH_VERIFY=1 \
  node scripts/build-desktop-package.mjs --target aarch64-apple-darwin
```

### 6.2 Object Storage

```bash
mc cp dist/releases/iClaw_1.0.1.202604061643_aarch64_prod.dmg prodavatar/iclaw-prod/darwin/aarch64/
mc cp dist/releases/iClaw_1.0.1.202604061643_aarch64_prod.dmg prodavatar/iclaw-prod/downloads/darwin/aarch64/

mc cp dist/releases/LiCaiClaw_1.0.1.202604061643_aarch64_prod.dmg prodavatar/licaiclaw-prod/darwin/aarch64/
mc cp dist/releases/LiCaiClaw_1.0.1.202604061643_aarch64_prod.dmg prodavatar/licaiclaw-prod/downloads/darwin/aarch64/
```

### 6.3 Home-Web

```bash
node scripts/run-with-env.mjs prod env \
  APP_NAME=iclaw \
  ICLAW_BRAND=iclaw \
  ICLAW_PORTAL_APP_NAME=iclaw \
  ICLAW_HOME_PUBLIC_RELEASE_VERSION=1.0.1.202604061643 \
  pnpm --dir home-web build
rsync -avz --delete home-web/dist/ root@113.44.132.75:/var/www/iclaw-home/

node scripts/run-with-env.mjs prod env \
  APP_NAME=licaiclaw \
  ICLAW_BRAND=licaiclaw \
  ICLAW_PORTAL_APP_NAME=licaiclaw \
  ICLAW_HOME_PUBLIC_RELEASE_VERSION=1.0.1.202604061643 \
  pnpm --dir home-web build
rsync -avz --delete home-web/dist/ root@113.44.132.75:/var/www/caiclaw/home/
```

## 7. 风险点

- `iclaw` 当前 runtime prod public base 仍非最终规范值，本次构建继续使用显式 runtime URL override；后续应把品牌 runtime 配置彻底修正
- `macOS x64` 当前不支持正式发版，本次只覆盖 `arm64`
- DMG Finder 布局定制阶段仍会出现 `不能获得 disk` 警告，但不影响签名、公证与 DMG 生成

## 8. 验证项

- [x] `iclaw` DMG 已生成并通过 notarization
- [x] `licaiclaw` DMG 已生成并通过 notarization
- [x] `https://iclaw.aiyuanxi.com/downloads/darwin/aarch64/iClaw_1.0.1.202604061643_aarch64_prod.dmg` 返回 `200 OK`
- [x] `https://caiclaw.aiyuanxi.com/downloads/darwin/aarch64/LiCaiClaw_1.0.1.202604061643_aarch64_prod.dmg` 返回 `200 OK`
- [x] `iclaw` 首页当前加载 JS 中 `release.version="1.0.1.202604061643"`
- [x] `licaiclaw` 首页当前加载 JS 中 `release.version="1.0.1.202604061643"`

## 9. 回滚方案

### 9.1 Desktop

- 将下载页回切到上一版 `1.0.1.202604050504`
- 如需回滚安装包对象，恢复上一版 DMG：
  - `iClaw_1.0.1.202604050504_aarch64_prod.dmg`
  - `LiCaiClaw_1.0.1.202604050504_aarch64_prod.dmg`

### 9.2 Home-Web

- 重新发布上一版 `home-web` 静态产物

## 10. 实际执行记录

- 开始时间：`2026-04-06 16:43:21 CST`
- 结束时间：`2026-04-06 16:50:04 CST`
- 实际执行人：`xingkaihan`
- 实际结果：
  - 已完成 `iclaw` / `licaiclaw` 的 `prod macOS arm64` 业务系统安装包打包
  - 两个品牌 DMG 均已通过 Apple notarization
  - 已上传两个品牌的 `prod darwin/aarch64` DMG 到各自下载桶
  - 已重新发布两个品牌的 `home-web prod`
  - 两个下载页当前都已切到最新对外版本 `1.0.1.202604061643`

## 11. 发布后补齐：control-plane 对齐最新 commit

- 时间：`2026-04-06 17:08:20 CST`
- 目标：
  - 将线上 `control-plane` 从 `71d3403` 对齐到当前仓库最新提交 `4de9cc3`
- 执行：
  - 从干净源码状态重新执行 `bash scripts/deploy-control-plane.sh prod`
  - 为本次补齐动作创建 tag：`release-1.0.1.202604061643-post1`
- 验证：
  - `https://iclaw.aiyuanxi.com/health`
  - `https://caiclaw.aiyuanxi.com/health`
  - 两侧均返回：
    - `git_commit=4de9cc3f0eb8227fbc9a7263060bfca53dc88c73`
    - `git_tag=release-1.0.1.202604061643-post1`
- 说明：
  - 本次未执行数据库发布，线上数据库仍沿用 `2026-04-05` 那轮“保留账号核心信息，其余按基线覆盖”的结果
  - 当前 `/health` 中 `release_version` 仍显示旧值 `1.0.1+202604021919`，说明后端 `build-info` 版本来源尚未完全切到四段对外版本规范
  - 当前 `/health` 中 `build_metadata.dirty=true`，说明 `write-build-info` 仍把本地工作区状态计入构建元数据；该问题不影响本次后端代码实际已对齐到最新 commit，但后续应单独修正
