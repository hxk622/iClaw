# 1.0.10.202604172041

## 1. 发布基线

- `version_no`: `1.0.10.202604172041`
- `environment`: `prod`
- `owner`: `admin`
- `window`: `2026-04-17 20:43 CST`
- `test_report`: `docs/version_record/test_report/1.0.10.202604172041.md`
- `status`: `draft`

## 2. 变更摘要

- Windows `caiclaw` prod 桌面包升级到 `1.0.10+202604172041`，并统一根包、桌面包、Tauri、Cargo 版本号。
- 发布 `control-plane` prod，并将 Windows x64 桌面发版记录切到 `1.0.10` 强更。
- 发布 `admin-web` prod。

## 3. 发布产物

- Windows x64 prod 安装包：`dist/releases/LiCaiClaw_1.0.10.202604172041_x64_prod.exe`
- 下载地址：`https://caiclaw.aiyuanxi.com/downloads/windows/x64/LiCaiClaw_1.0.10.202604172041_x64_prod.exe`
- Windows manifest：`https://caiclaw.aiyuanxi.com/downloads/windows/x64/latest-prod-windows-x64.json`

## 4. 验证结果

- `control-plane` prod `/health` 返回 200，`release_version=1.0.10.202604172035`。
- 安装包下载地址返回 200，`Content-Length=189174844`。
- 使用 `1.0.9+202604171540` 请求头访问线上接口时，返回：
- `x-iclaw-update-available=true`
- `x-iclaw-update-mandatory=true`
- `x-iclaw-update-enforcement-state=required_now`
- `x-iclaw-update-artifact-url=https://caiclaw.aiyuanxi.com/desktop/release-file?app_name=caiclaw&channel=prod&target=windows&arch=x64&artifact_type=installer`

## 5. 风险与备注

- `release-preflight.mjs` 在本机直接执行会因为 `pnpm` 不在 PATH 中失败；本次通过 `corepack pnpm` 与现有发布脚本完成替代校验和构建。
- `publish-downloads.sh` 当前会保留最近 2 个 Windows prod 安装包，因此对象存储中同时存在 `1.0.9` 与 `1.0.10`。
