# 1.0.5.202604130925

## 1. 发布基线

- `version_no`: `1.0.5.202604130925`
- `environment`: `prod`
- `public_release_version`: `1.0.5.202604130925`
- `package_version`: `1.0.5+202604121515`
- `rollback_version`: `1.0.2.202604090006`
- `owner`: `xingkaihan`
- `window`: `2026-04-13 09:25 CST`
- `test_report`: `docs/version_record/test_report/1.0.5.202604130925.md`
- `status`: `completed`

## 2. 变更摘要

- 重新完成 `iClaw` / `LiCaiClaw` macOS arm64 prod 厚包发版。
- 正式安装包内置 `runtime-archives/openclaw-runtime-aarch64-apple-darwin-2026.3.13.tar.gz`。
- 修复 macOS 公证链路：
  - 不再依赖 Tauri 内置阻塞式 notarization
  - 改为脚本接管 submit / poll / staple
  - 对 bundled runtime archive 内部 Mach-O / Python framework 做补签名
- 修复发布脚本：
  - `build-desktop-matrix.sh` 兼容四段版本命名后的 DMG 发现
  - `publish-downloads.sh` 修复 `mc alias` 检测
  - `publish-downloads.sh` 改为基于本地 `dist/releases` 生成 manifest

## 3. 发布产物

- `iClaw`:
  - `https://iclaw.aiyuanxi.com/downloads/mac/aarch64/iClaw_1.0.5.202604130925_aarch64_prod.dmg`
- `LiCaiClaw`:
  - `https://caiclaw.aiyuanxi.com/downloads/mac/aarch64/LiCaiClaw_1.0.5.202604130925_aarch64_prod.dmg`

## 4. 验证结果

- `iClaw` prod DMG URL 返回 `HTTP 200`
- `LiCaiClaw` prod DMG URL 返回 `HTTP 200`
- `iClaw` prod manifest 已更新到 `release_version = 1.0.5.202604130925`
- `LiCaiClaw` prod manifest 已更新到 `release_version = 1.0.5.202604130925`
- 两个 OEM 的 `latest-prod.json` / `latest-prod-mac-aarch64.json` 已分别回到各自品牌真值

## 5. 风险与备注

- 当前公开 manifest 仍保留 `version = 1.0.5+202604121515` 作为包内版本，下载文件名与对外发布版本使用四段点号版本。
- 本次 `publish-downloads.sh` 仍会上传历史 updater 产物；正式真值已切为 installer-only，后续应继续收敛脚本，避免上传无用 updater 文件。
