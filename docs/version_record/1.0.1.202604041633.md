# 1.0.1.202604041633

## 1. 发布基线

- `version_no`: `1.0.1.202604041633`
- `release_id`: `release-2026-04-04-01`
- `environment`: `prod`
- `git_tag`: `待冻结`
- `git_commit`: `待冻结`
- `rollback_tag`: `待确认`
- `owner`: `待指定`
- `reviewer`: `待指定`
- `window`: `待指定`
- `status`: `draft`

## 2. 变更摘要

- 本次发布目标：把当前一批桌面端、控制面和 OEM 运行时相关改动，按统一基线一次性发布到 `prod`
- 用户可见变化：预期覆盖桌面端、控制面、OEM 配置和下载链路的同步发布
- 风险最高的点：代码、数据库、对象存储和 OEM runtime 配置跨层联动，若顺序错误容易出现“代码已发但配置未生效”

## 3. 发布范围

### 3.1 Code

- [x] `control-plane`
- [x] `admin-web`
- [x] `home-web`
- [x] `desktop`
- [ ] 其他：

说明：

- 这次讨论里，存在多个横跨 `desktop / control-plane / admin-web / home-web` 的改动
- 冻结时必须以 `git diff <rollback_tag>..<git_tag>` 为准重新核对

### 3.2 DB Schema

- [x] 无
- [ ] 有

内容：

- 当前未确认有 schema 变更
- 若最终基线内包含 migration、索引或表结构调整，必须补充到本节

### 3.3 DB Data

- [ ] 无
- [x] 有

内容：

- OEM runtime / welcome 配置修正
- preset 同步
- 可能存在模型中心、记忆配置、品牌配置等数据变更

待确认项：

- 这次基线是否包含需要同步到 `prod` 的模型中心配置
- 这次基线是否包含需要同步到 `prod` 的 Memory provider 配置
- 这次基线是否包含 OEM shell / welcome / 输入框配置数据

### 3.4 Object Storage

- [ ] 无
- [x] 有

内容：

- OEM 资源
- desktop 下载产物
- 可能存在 runtime 资源同步

待确认项：

- 本次是否需要同步 `MinIO` 全量桶
- 本次是否需要只同步新增对象
- 本次是否需要发布新的 installer / updater / manifest

### 3.5 Infra Config

- [x] 无
- [ ] 有

内容：

- 当前暂未确认有新的 infra 配置
- 若正式执行前涉及 Nginx path / 域名 / HTTPS / PM2 配置调整，必须补充到本节

## 4. 发布前检查

- [ ] 已冻结 `git tag + commit`
- [ ] 已确认发布范围
- [ ] 已确认回滚目标
- [ ] 已确认执行人和复核人
- [ ] 已确认依赖服务可用
- [ ] 已确认脚本默认目标环境正确

## 5. 执行顺序

1. 冻结基线
2. 执行 `DB Schema`
3. 执行 `DB Data`
4. 发布 `control-plane`
5. 发布 `admin-web`
6. 发布 `home-web`
7. 同步 `MinIO / S3`
8. 发布 desktop 安装包 / updater / 下载页
9. 执行 smoke test
10. 记录结果

## 6. 执行命令

### 6.1 DB Schema

```bash
# 当前待确认是否有 schema 变更
```

### 6.2 DB Data

```bash
# 根据冻结后的基线决定是 migration / data patch / preset sync
```

### 6.3 Backend

```bash
bash scripts/deploy-control-plane.sh prod
```

注意：

- 当前脚本只同步 `src/`、`scripts/`、`sql/`
- 若本次发布包含 `services/control-plane/presets/`，必须先补脚本或单独同步

### 6.4 Frontend

```bash
bash scripts/deploy-admin.sh prod
bash scripts/deploy-home.sh prod
```

### 6.5 Object Storage / Desktop

```bash
bash scripts/sync-minio-to-prod.sh
pnpm publish:desktop-release
```

注意：

- 必须先确认本次需要同步的 bucket
- 不允许盲目全量覆盖
- 如果本次需要重新打包 desktop，也应先完成矩阵构建和上传清单确认

## 7. 风险点

- `control-plane` 发布脚本当前未覆盖 `presets/`，容易导致代码和 preset 不一致
- 数据库同步脚本偏粗，不适合所有发布都直接整段恢复
- 当前桌面端 / OEM / 资源链路跨 `DB + code + asset`，如果不按顺序发，极易出现“代码已改但配置未生效”

## 8. 验证项

- [ ] `control-plane /health`
- [ ] `admin-web` 正常打开
- [ ] `home-web` 正常打开
- [ ] `portal/public-config?app_name=<brand>` 返回预期配置
- [ ] 品牌 welcome 页资源清晰可见
- [ ] 登录 / 注册正常
- [ ] chat 发送一条真实消息成功
- [ ] usage / 计费 / 余额变更正确
- [ ] desktop 下载页和安装包链接正常
- [ ] updater manifest 可读取

## 9. 回滚方案

### 9.1 Code

```bash
# 使用 rollback_tag 对应版本重新发布
```

### 9.2 DB

```sql
-- 若为 migration，按 migration 回滚策略处理
-- 若为 data patch，执行对应反向 SQL 或恢复备份
```

### 9.3 Object Storage

- 恢复上一版对象
- 恢复上一版下载清单

## 10. 实际执行记录

- 开始时间：
- 结束时间：
- 实际执行人：
- 实际结果：
- 偏差说明：
